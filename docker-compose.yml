version: '3'
services:
  clickshop:
    build: .
    ports:
      - "5006:5006"
      - "8000:8000"  # metrics
  prometheus:
    image: prom/prometheus
    ports:
      - "9090:9090"
    volumes:
      - ./prometheus.yml:/etc/prometheus/prometheus.yml
    depends_on:
      - clickshop
